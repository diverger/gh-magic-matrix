{
  "name": "gh-magic-matrix",
  "private": true,
  "version": "1.3.2",
  "workspaces": [
    "packages/*",
    "packages/snake/packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
