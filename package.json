{
  "name": "gh-magic-matrix",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "packages/snake/packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
