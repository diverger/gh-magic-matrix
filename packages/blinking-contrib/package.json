{
  "name": "@gh-magic-matrix/blinking-contrib",
  "version": "1.0.0",
  "description": "GitHub Action to create blinking contribution visualization - displays each year's contributions with fade transitions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "ncc build src/action.ts -o dist --minify && tsc --emitDeclarationOnly --declaration --outDir dist",
    "test": "echo \"No tests yet\""
  },
  "keywords": [
    "github",
    "contributions",
    "animation",
    "svg",
    "blinking"
  ],
  "author": "diverger",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}
